---
import Layout from '../../layouts/Layout.astro';
import { getCollection } from 'astro:content';
import { categoriesNotes } from '../../consts';

const notes = (await getCollection('notes')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);
---

<Layout title="заметки про разработку программного обеспечения, образование и IT в целом" description="Погрузитесь в мир разработки. Узнайте что такое технический долг, качества хорошего разработчика и важность развития логического мышления. Улучшайте свои навыки программирования!" keywords="PEP 8, разработка, технический долг, программирование, качества разработчика, логическое мышление, python, javascript, php, code monkey, фундаментальные знания">	
	<h3 class="text-4xl lg:text-5xl text-left py-14   border-b-2 border-solid border-black">Заметки разработчика</h3>
	<div class="text-left py-10 border-b-2 border-solid border-black py-10">{
		Object.keys(categoriesNotes).map((category, i) => (
			<a href=`/notes/categories/${category}/` class="bg-gray-200 px-3 py-1 mr-2 no-underline text-sm">{categoriesNotes[category].title}</a>
		))
	} </div>
	<div>
	{
		notes.map((note, index) => (
			<div class=`lg:flex ${index < notes.length - 1 ? "border-b border-dashed border-slate-500" : ""} py-4 items-center`>
				<div class="lg:w-9/12 w-full pb-0 pt-0">
					<a href=`/notes/${note.slug}/` class="text-xl no-underline hover:underline">{note.data.title}</a>
				</div>
				<div class="lg:w-3/12 mt-4 lg:mt-0 w-full lg:text-right pb-0 pt-0">
					{
						note.data.categories.map((category) => (
								<a href=`/notes/categories/${category}/` class="bg-gray-200 px-3 py-1 lg:ml-2 no-underline text-sm">
									{categoriesNotes[category]['title']}
								</a>
							)
						)
					}
				</div>
			</div>
		))
	}
	</div>
</Layout>